1.将一个React元素渲染到根节点，只需要将其传入ReactDom.render()

2.React元素的更新只会更新需要改变的部分，它会比较元素之前的状态

3.组件的理解，类似于js的函数。它接收任意的入参props，返回用于描述页面展示内容的React元素
  其中的组件复用，提取
  !提取组件这里不熟悉

4.State和生命周期

5.事件处理
React 事件的命名采用小驼峰式（camelCase），而不是纯小写。
使用 JSX 语法时你需要传入一个函数作为事件处理函数，而不是一个字符串。